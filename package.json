{
  "name": "switchboard",
  "version": "0.1.0",
  "private": true,
  "description": "A dispatcher for many Claude agents at once, one per git worktree.",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @switchboard/shared build && pnpm --parallel --filter \"./{shared,server,web}\" dev",
    "dev:server": "pnpm --filter @switchboard/server dev",
    "dev:web": "pnpm --filter @switchboard/web dev",
    "build": "pnpm --filter @switchboard/shared build && pnpm --filter @switchboard/web build && pnpm --filter @switchboard/server build",
    "swb": "node cli/bin/swb.js",
    "start": "node cli/bin/swb.js start",
    "stop": "node cli/bin/swb.js stop",
    "restart": "node cli/bin/swb.js restart",
    "pull": "node cli/bin/swb.js pull",
    "status": "node cli/bin/swb.js status",
    "scratch": "node cli/bin/swb.js scratch",
    "password": "node cli/bin/swb.js password",
    "typecheck": "pnpm --filter @switchboard/shared build && pnpm -r typecheck",
    "test": "pnpm --filter @switchboard/shared build && vitest run",
    "test:watch": "pnpm --filter @switchboard/shared build && vitest",
    "coverage": "pnpm --filter @switchboard/shared build && vitest run --coverage",
    "postinstall": "node cli/bin/swb.js ensure-native",
    "ensure-native": "node cli/bin/swb.js ensure-native"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
